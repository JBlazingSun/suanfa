<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blazings.suanfa.sql.tujie.dao.ScoreDao">

    <resultMap type="com.blazings.suanfa.sql.tujie.entity.Score" id="ScoreMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="stuId" column="stu_id" jdbcType="INTEGER"/>
        <result property="courseId" column="course_id" jdbcType="INTEGER"/>
        <result property="score" column="score" jdbcType="VARCHAR"/>
    </resultMap>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into tujie-sql.Score(stu_id, course_id, score)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.stuId}, #{entity.courseId}, #{entity.score})
        </foreach>
    </insert>
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into tujie-sql.Score(stu_id, course_id, score)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.stuId}, #{entity.courseId}, #{entity.score})
        </foreach>
        on duplicate key update
        stu_id = values(stu_id) , course_id = values(course_id) , score = values(score)
    </insert>

</mapper>

